# CLI entry point skeleton
# This will be refined as the library evolves.

#include <corridorkey/api_export.hpp>
#include <iostream>

int main(int argc, char* argv[]) {
    (void)argc;
    (void)argv;
    
    std::cout << "CorridorKey Runtime CLI (Skeleton)" << std::endl;
    return 0;
}
